import React from "react";
import { Card, CardHeader, CardTitle } from "reactstrap";

type Props = {};

export default function ImporterListContainer({}: Props) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>لیست شرکت های وارد کننده</CardTitle>
      </CardHeader>
    </Card>
  );
}
