import React from "react";
import { Card, CardHeader, CardTitle } from "reactstrap";

type Props = {};

export default function LiveStockList({}: Props) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>لیست دامداری ها</CardTitle>
      </CardHeader>
    </Card>
  );
}
